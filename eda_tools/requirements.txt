# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# EDA Tools
# skidl 2.2.1's KiCad 9 schematic generator is a no-op stub; 2.2.3 implements it
skidl==2.2.3

# KiCad MCP schematic generation (default backend, SKiDL stays as fallback).
# mcp must stay <2: the vendored MCP server wheel uses the mcp 1.x low-level
# Server API which mcp 2.x removed.
mcp>=1.0.0,<2
kicad-sch-api>=0.5.0
# Vendored wheel = upstream circuit-synth/mcp-kicad-sch-api tag v0.2.2
# (wheel lives in that repo's dist/) plus our patch: add_component accepts a
# "unit" argument so multi-unit symbols (LM358 dual opamp, ...) can place
# every unit under one reference.
./wheels/mcp_kicad_sch_api-0.2.2-py3-none-any.whl

# KiCad integration (requires KiCad to be installed)
# Python bindings for KiCad - NOT installed via pip
# pcbnew comes from KiCad installation
# Uncomment the line below if you have KiCad installed with Python bindings
# pcbnew  # This comes from KiCad installation

# Circuit processing
numpy==1.26.3
scipy==1.11.4
matplotlib==3.8.2

# File processing
Pillow==10.2.0

# BOM generation
pandas==2.1.4
openpyxl==3.1.2

# Utilities
python-dotenv==1.0.0
pydantic>=2.10.0
httpx==0.26.0

# SVG generation for schematics
svgwrite==1.4.3

# Note: KiCad must be installed separately on the system
# Windows: https://www.kicad.org/download/
# Python bindings are included with KiCad installation
